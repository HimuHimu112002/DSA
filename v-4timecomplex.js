function max(n) {
    for (let index = 0; index <= Math.max(5, n); index++) {
        console.log(index)
        
    }
}
//max(10)
// depend on (n) so time complexity depend korbe (n) er upore
function min(n) {
    for (let index = 0; index <= Math.min(5, n); index++) {
        console.log(index)
        
    }
}
min(100)
// depend on (min) so time complexity depend korbe (min) er upore so time complexity 1